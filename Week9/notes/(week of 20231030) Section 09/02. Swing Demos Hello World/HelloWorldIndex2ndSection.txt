V10:  Moved layout code into a layoutComponents() method, 
      moved plumbing code into a addListeners method. 
      Show button processing. 
      Uses the "ActionListener is the JFrame class" strategy.
      
V11:  Uses the "ActionListener is an inner class" strategy.
      
V12:  Uses the "ActionListener is an anonymous inner class" strategy.
