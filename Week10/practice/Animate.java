import java.awt.*;
import javax.swing.*;

public class Animate {
    public static void main(String[] args) {
        JFrame j = new JFrame("Animation");
        j.setSize(1000, 1000);
        j.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);

        JLabel jl = new JLabel("The Zenana");
        jl.setFont(new Font("SansSerif", Font.PLAIN, 18));
        
        j.add(jl, BorderLayout.NORTH);

        j.setVisible(true);

        for (int repetition = 0; repetition < 10; repetition++) {
            try {
                
            } catch () {
                
            }
        }
    }
}